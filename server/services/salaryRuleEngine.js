/**
 * Salary Rule Engine
 * Computes payslip lines from salary rules for a given employee/contract.
 * TODO: implement formula parser and computation logic
 */

const compute = async (contract, attendanceSummary) => {
  // Returns array of { name, code, category, amount }
  throw new Error('Salary rule engine not yet implemented')
}

module.exports = { compute }
