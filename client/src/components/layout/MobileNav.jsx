/**
 * Mobile navigation is handled via the Sidebar component with the
 * sidebarOpen state from AppContext. The Topbar renders a hamburger
 * button on small screens that toggles the sidebar overlay.
 */
export default function MobileNav() {
  return null
}
